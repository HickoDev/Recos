CISCO_CLIENT_ID=
CISCO_CLIENT_SECRET=

SMTP_HOST=
SMTP_PORT=587
SMTP_USER=
# Wrap in quotes if it contains spaces (e.g., Gmail app password)
SMTP_PASS=
MAIL_FROM=
MAIL_TO=

# Dashboard auth (sessions)
# Set a strong random value for SECRET_KEY (used to sign session cookies)
SECRET_KEY=change-me-to-a-random-string
# Admin username and password for login. For production, prefer a hashed password:
#   ADMIN_PASSWORD=pbkdf2:<hex>
# To generate: python - <<'PY'\nimport os,hashlib;u='admin';p='YourPassword';print('pbkdf2:'+hashlib.pbkdf2_hmac('sha256',p.encode(),u.encode(),100000).hex())\nPY
ADMIN_USER=admin
# Plaintext (dev): set a simple password
ADMIN_PASSWORD=admin
# Or prefer a hashed password in production:
# ADMIN_PASSWORD=pbkdf2:<hex>
